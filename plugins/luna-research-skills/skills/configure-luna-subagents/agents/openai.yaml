interface:
  display_name: "Configure Luna Subagents"
  short_description: "Configure Codex-native Luna subagent defaults"
  default_prompt: "Use $configure-luna-subagents to inspect, migrate, or safely configure Codex-native Luna subagent defaults."

policy:
  allow_implicit_invocation: false
